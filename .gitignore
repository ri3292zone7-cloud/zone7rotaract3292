# OS / editor junk
Thumbs.db
Desktop.ini
.DS_Store
*.log

# Vercel / local deployment state
.vercel/
node_modules/

# Python local server artifacts
__pycache__/
*.pyc

# Local backup of the old site (keep out of the repo)
../zone7-backup-old-site/

# Raw source media (AI chapter images, store/vendor clips) — referenced by nothing,
# kept only as build source. The pre-reorg backup holds the originals.
_source-media/
